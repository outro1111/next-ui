/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["eiliehkfifbqifmsqxcs.supabase.co"]
  },
};

module.exports = {
  // Other configuration settings
  output: 'export',
}

export default nextConfig;
